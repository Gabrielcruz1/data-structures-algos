// INDEX OF . Returns the index of a given element of the array, Can add a comma and a number to start from a certain index. 
const beasts = ['ant', 'bison', 'camel', 'duck', 'bison'];
console.log(beasts.indexOf("bison", 3 ))

//FOR OF . ITERATOR LIKE A FOR LOOP 
const array1 = ['a', 'b', 'c'];

for (let pizza of array1) {
      console.log(pizza);
};

//SORT METHOD 
// the built in sort method accepts an optional comparator funtion. 